# Anyon Environment Variables Example
# 이 파일은 팀원들과 공유하기 위한 템플릿입니다.
# 실제 개발 시: 이 파일을 .env로 복사하고 실제 값을 입력하세요.
#
# 복사 명령:
#   cp .env.example .env

# ==========================================
# 🔴 필수: 암호화 키 (절대 공유 금지!)
# ==========================================
# 생성 방법: openssl rand -base64 32
ANYON_SECRET_KEY=your-secret-key-here

# ==========================================
# 🟢 서버 설정
# ==========================================
# 백엔드 포트 (기본값: 0 = 자동 할당)
BACKEND_PORT=3001

# 호스트 (기본값: 127.0.0.1)
# 외부 접속을 허용하려면 0.0.0.0으로 변경
HOST=127.0.0.1

# 로그 레벨 (기본값: info)
# 옵션: trace, debug, info, warn, error
RUST_LOG=debug

# ==========================================
# 🟡 프론트엔드 개발 서버
# ==========================================
# Vite 개발 서버 포트
FRONTEND_PORT=3000

# 백엔드 호스트 (프록시용)
BACKEND_HOST=localhost

# 브라우저 자동 열기
VITE_OPEN=false

# ==========================================
# 📁 디렉토리 설정 (선택사항)
# ==========================================
# 주석 처리하면 기본값 사용:
# - ANYON_ASSET_DIR: <프로젝트>/dev_assets
# - ANYON_TEMP_DIR: /var/tmp/anyon-dev
# - ANYON_WORKTREE_DIR: $ANYON_TEMP_DIR/worktrees
# ANYON_ASSET_DIR=/path/to/custom/data
# ANYON_TEMP_DIR=/path/to/custom/temp
# ANYON_WORKTREE_DIR=/path/to/custom/worktrees

# ==========================================
# 📊 분석 (선택사항)
# ==========================================
# PostHog 분석을 사용하려면 주석 해제
# POSTHOG_API_KEY=phc_your_key_here
# POSTHOG_API_ENDPOINT=https://app.posthog.com

# 프론트엔드용 PostHog
# VITE_POSTHOG_API_KEY=phc_your_key_here
# VITE_POSTHOG_API_ENDPOINT=https://app.posthog.com

# ==========================================
# 🔐 GitHub OAuth (필수)
# ==========================================
# GitHub OAuth App Client ID
# 개인 OAuth App을 생성하거나 기본 제공되는 앱 사용
# 생성 방법: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id_here

# GitHub Client Secret (Device Flow에서는 사용 안 됨)
# Standard OAuth Flow로 전환 시 필요
# GITHUB_CLIENT_SECRET=your_github_client_secret_here

# ==========================================
# 🔐 GitHub App (선택사항, 빌드 타임)
# ==========================================
# GitHub App을 사용하는 경우 설정
# GITHUB_APP_ID=your_app_id_here
# GITHUB_APP_CLIENT_ID=your_app_client_id_here

# ==========================================
# 🤖 Claude CLI (선택사항)
# ==========================================
# Claude CLI 로그인 명령 (기본값: "npx -y @anthropic-ai/claude-code@2.0.31 login")
# CLAUDE_LOGIN_COMMAND=npx -y @anthropic-ai/claude-code@latest login

# ==========================================
# 🐛 디버깅 (선택사항)
# ==========================================
# Worktree 자동 정리 비활성화 (디버깅용)
# DISABLE_WORKTREE_ORPHAN_CLEANUP=1
